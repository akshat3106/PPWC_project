# Base URL of the backend API. Leave unset for local dev — Vite's dev server
# proxies /api/* to http://localhost:5000 automatically (see vite.config.ts).
#
# Set this when running against a deployed backend, e.g. the Cloud Run service:
# VITE_API_URL=https://attendly-backend-588832953009.asia-south1.run.app
